const PREFIX = 'PERSON';

const ActionType = {
  SET_PERSON: `${PREFIX}/SET_PERSON`,
  SET_LOADING: `${PREFIX}/SET_LOADING`,
  SET_ERROR: `${PREFIX}/SET_ERROR`,
  RESET_PERSON: `${PREFIX}/RESET_PERSON`,
};

export default ActionType;
