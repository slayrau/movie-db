const PREFIX = 'MOVIES';

const ActionType = {
  SET_MOVIES: `${PREFIX}/SET_MOVIES`,
  SET_LOADING: `${PREFIX}/SET_LOADING`,
  SET_ERROR: `${PREFIX}/SET_ERROR`,
  RESET_MOVIES: `${PREFIX}/RESET_MOVIES`,
};

export default ActionType;
